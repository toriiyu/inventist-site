/* Cloudflare Pages Function: POST /api/contact */
function json(obj,status){return new Response(JSON.stringify(obj),{status:status||200,headers:{'Content-Type':'application/json','Cache-Control':'no-store'}});}
export async function onRequestPost(context){const {request,env}=context;try{const ct=request.headers.get('content-type')||'';let data={};if(ct.indexOf('application/json')>=0){data=await request.json();}else{const fd=await request.formData();fd.forEach(function(v,k){data[k]=v;});}if(data._gotcha)return json({ok:true});if(!data.email||!data.name)return json({ok:false,error:'missing_fields'},400);const to=env.CONTACT_TO;const key=env.RESEND_API_KEY;if(!to||!key)return json({ok:false,error:'not_configured'},200);const subject=data._subject||'inventist.jp inquiry';const order=['name','company','email','tel','type','want','deal_type','field','patent_no','message'];let lines=[];order.forEach(function(k){if(data[k])lines.push(k+': '+data[k]);});Object.keys(data).forEach(function(k){if(k[0]!=='_'&&order.indexOf(k)<0&&k!=='consent')lines.push(k+': '+data[k]);});if(data._page)lines.push('(page: '+data._page+')');const text=lines.join('
');const from=env.CONTACT_FROM||'inventist.jp <onboarding@resend.dev>';const resp=await fetch('https://api.resend.com/emails',{method:'POST',headers:{'Authorization':'Bearer '+key,'Content-Type':'application/json'},body:JSON.stringify({from:from,to:[to],reply_to:data.email,subject:subject,text:text})});if(resp.ok)return json({ok:true});return json({ok:false,error:'send_failed'},200);}catch(e){return json({ok:false,error:'exception'},200);}}
export async function onRequestGet(){return json({ok:true,service:'inventist contact',method:'POST only'});}
